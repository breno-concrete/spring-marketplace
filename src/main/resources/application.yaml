spring:
  application:
    name: marketplace-test
  datasource:
    url: jdbc:postgresql://localhost:5332/breno
    password: password
    username: breno
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true
